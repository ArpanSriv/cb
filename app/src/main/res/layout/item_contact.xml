<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:background="@color/primaryColor">

    <ImageView
        android:id="@+id/contact_imageView"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/contactName_TextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No name found"
        android:textSize="16sp"
        android:layout_marginLeft="16dp"
        app:layout_constraintBottom_toTopOf="@+id/contactNumber_TextView"
        app:layout_constraintLeft_toRightOf="@id/contact_imageView"
        app:layout_constraintStart_toEndOf="@+id/contact_imageView"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        app:layout_constraintVertical_chainStyle="spread" />

    <TextView
        android:id="@+id/contactNumber_TextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No number found"
        android:textSize="14sp"
        app:layout_constraintHorizontal_bias="0"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/contactName_TextView"
        app:layout_constraintStart_toStartOf="@+id/contactName_TextView"
        app:layout_constraintTop_toBottomOf="@+id/contactName_TextView" />

</android.support.constraint.ConstraintLayout>